# Module Resolution

How TS finds modules. `node` resolution (looks in `node_modules`) vs `classic`. Configured via `moduleResolution`.

> 📘 Next: [Ambient Declarations (declare)](04-ambient-declarations-declare.md)
