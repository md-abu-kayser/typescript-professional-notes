# Vite & esbuild Integration

Vite uses esbuild for fast TS transpilation (type stripping). Type checking via `vue-tsc` or `tsc --noEmit`.

> 📘 Next: [Babel TypeScript Preset](07-babel-typescript-preset.md)
