# tsc Compiler Overview

`tsc` compiles TS to JS. Use `tsc --init` to create `tsconfig.json`. Key flags: `--watch`, `--project`, `--noEmit`.

> 📘 Next: [Editor Setup (VS Code)](05-editor-setup-vscode.md)
