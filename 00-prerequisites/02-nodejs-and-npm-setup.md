# Node.js & npm Setup

Install Node.js LTS, verify `node -v` and `npm -v`. Initialize project with `npm init`. Understanding `package.json`.

> 📘 Next: [Installing TypeScript](03-installing-typescript.md)
