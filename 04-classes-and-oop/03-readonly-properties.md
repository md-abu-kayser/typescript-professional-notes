# Readonly Properties

`readonly name: string;` or in constructor parameter: `constructor(public readonly name: string)`. Can only set in constructor.

> 📘 Next: [Constructors & Parameter Properties](04-constructors-and-parameter-properties.md)
