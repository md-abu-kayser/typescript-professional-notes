# Arrays and Tuples

Arrays: `let list: number[] = [1,2];` or `Array<number>`. Tuples: fixed-length with specific types: `let x: [string, number] = ['hello', 10];`.

> 📘 Next: [Enums](04-enums-string-enums-const-enums.md)
