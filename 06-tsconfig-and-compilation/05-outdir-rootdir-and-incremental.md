# outDir, rootDir & Incremental

`outDir` output directory. `rootDir` root of source. `incremental: true` speeds rebuilds.

> 📘 Next: [declaration & declarationMap](06-declaration-and-declarationMap.md)
